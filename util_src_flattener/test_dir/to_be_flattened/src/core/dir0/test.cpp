// This include is relative to the include path which will include a library's /include folder as is standard
// It will be changed to refer to the flattened header after running the flattener tool
#include <core/dir0/test.h>